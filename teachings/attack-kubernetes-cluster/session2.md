# Session 2

This session is less guided than the previous one, as the concepts and the work
to do is less complicated.

## Goals of the session:

- Create a python script that will server as backend: a HTTP server which returns a flag (specific character chain) when queried.